<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sky.mapper.MessageMapper">

    <!-- 分页查看所有反馈信息 -->
    <select id="selectFeedbacks" parameterType="com.sky.dto.FeedBacksPageQueryDTO" resultType="com.sky.vo.FeedBacksVO">
        SELECT
        f.feedback_id AS feedbackId,
        f.type AS type,
        f.feedback_title AS title,
        f.feedback_text AS content,
        u.username AS username,
        u.phone_number AS phoneNumber,
        f.feedback_time AS feedbackTime,
        r.reply_time AS replyTime,
        ru.username AS replyPerson,
        f.status AS status
        FROM
        FeedBacks f
        -- 连接 Users 表获取反馈用户信息
        JOIN
        Users u ON f.user_id = u.user_id
        -- 左连接 Replies 表获取回复信息
        LEFT JOIN
        Replies r ON f.feedback_id = r.feedback_id
        -- 左连接 Users 表获取回复人信息
        LEFT JOIN
        Users ru ON r.user_id = ru.user_id
        <where>
            <!-- 根据 status 条件筛选 -->
            <if test="status != null and status != ''">
                AND f.status = #{status}
            </if>
            <!-- 根据 type 条件筛选 -->
            <if test="type != null and type != ''">
                AND f.type = #{type}
            </if>
        </where>
        ORDER BY
        f.feedback_id DESC
    </select>

    <!-- 删除反馈信息 -->
    <delete id="deleteFeedback" parameterType="java.lang.Integer">
        DELETE FROM parking.feedbacks
        WHERE feedback_id = #{feedbackId}
    </delete>

    <!-- 用户创建反馈信息 -->
    <insert id="createFeedbacks" parameterType="com.sky.dto.FeedBacksDTO">
        INSERT INTO parking.feedbacks (user_id, feedback_title, feedback_text,  type, feedback_time)
        VALUES (#{userId}, #{feedbackTitle}, #{feedbackText}, #{type}, NOW())
    </insert>

<!--    <select id="getById" parameterType="java.lang.Integer" resultType="com.sky.entity.FeedBacks">-->
<!--        SELECT * FROM parking.feedbacks WHERE feedback_id = #{feedbackId}-->
<!--    </select>-->
</mapper>
